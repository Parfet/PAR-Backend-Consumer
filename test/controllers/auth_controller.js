const assert = require("assert");

const register = async () => {
  const req = {};
  describe("#Register", () => {
    it("normal register", () => {
        res.body = {
            username: 'usertest',
            email: 'test@test.com',
            provider: 'google.com',
            
        }
    });
  });
};

const authControllerTest = async () => {
  describe("Auth Controller", () => {});
};

module.exports = authControllerTest;
