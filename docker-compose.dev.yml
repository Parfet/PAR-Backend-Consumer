version: '3.8'
services:
 par-api:
    container_name: parfet-api-consumer-dev
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - '8001:8001'
    env_file:
      - .env
